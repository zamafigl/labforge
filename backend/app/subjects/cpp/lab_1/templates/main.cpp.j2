#include <iostream>
using namespace std;

int main() {
    int arr[] = {1, 3, 2, 5, 4};
    int count = 0;

    for (int i = 1; i < 5; i++) {
        if (arr[i] > arr[i - 1]) {
            count++;
        }
    }

    cout << "Result: " << count << endl;
    return 0;
}